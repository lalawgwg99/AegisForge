[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aegisforge"
version = "0.1.0"
description = "Agent Reliability OS MVP"
requires-python = ">=3.9"

[project.scripts]
aegisforge = "aegisforge.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
